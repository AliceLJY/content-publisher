# Layout Themes Reference

## Theme System Overview

The publishing pipeline supports 20 total themes: 3 baoyu built-in + 17 custom themes.

### baoyu Built-in Themes (NOT in rotation)

| Theme Key | Name | Notes |
|-----------|------|-------|
| default | Default | baoyu's default styling |
| grace | Grace | Elegant serif theme |
| simple | Simple | Minimal theme |

These exist in baoyu-skills and can be used manually, but they are excluded from automatic rotation.

### 17 Custom Themes (in rotation)

| # | Theme Key | Display Name |
|---|-----------|-------------|
| 01 | wechat-default | Default WeChat |
| 02 | wechat-tech | Tech Style |
| 03 | wechat-elegant | Elegant Minimal |
| 04 | wechat-deepread | Deep Reading |
| 05 | latepost-depth | LatePost Depth |
| 06 | wechat-nyt | New York Times |
| 07 | wechat-ft | Financial Times |
| 08 | wechat-jonyive | Jony Ive |
| 09 | wechat-medium | Medium |
| 10 | wechat-apple | Apple Minimal |
| 11 | wechat-anthropic | Claude |
| 12 | xiaomuwu-journal | Xiaomuwu Journal |
| 13 | sunset-orange | Sunset Orange |
| 14 | matcha-latte | Matcha Latte |
| 15 | sakura-letter | Sakura Letter |
| 16 | vibecoding-tech | VibeCoding |
| 17 | ai-custom | AI Custom (one-time) |

Full catalog with mood tags: see `layout-style-catalog.md` at repo root.

### ai-custom (#17)

When this theme is selected in rotation, the AI analyzes the article's tone, topic, and mood, then generates a one-time custom CSS theme. This theme is used once and not saved for reuse.

## Auto-Rotation Logic

1. Read `~/.openclaw-antigravity/workspace/images/layout-style-history.txt`
2. Find last used theme number
3. Next = last + 1 (wraps to 1 after 17)
4. Check tone match against article category/mood
5. If mismatch, skip to next
6. Pass `--theme <theme-key>` to the publish command

### History File Format

```
YYYY-MM-DD article-slug #NN theme-key display-name
```

Example:
```
2026-02-28 ai-agent-workflow #11 wechat-anthropic Claude
2026-03-01 wechat-publishing #12 xiaomuwu-journal Xiaomuwu Journal
```

## CSS Theme Generation

Custom theme CSS files are generated by `scripts/generate-layout-themes.ts`:

```bash
bun scripts/generate-layout-themes.ts
```

This script:
1. Reads the STYLES object from the editor HTML (`wechat_editor_xiaoshiai.html`)
2. Converts inline style definitions to CSS files
3. Writes them to `dependencies/baoyu-skills/skills/baoyu-post-to-wechat/scripts/md/themes/`
4. Skips default/grace/simple (would conflict with baoyu built-ins)
5. Generates `layout-style-catalog.md` at repo root

## Inline Formatting

`scripts/format-wechat.ts` applies inline styles directly to HTML elements (WeChat strips `<style>` tags). It:
- Parses frontmatter for title/author/summary
- Renders markdown via markdown-it with highlight.js
- Applies the selected theme's inline styles to every element
- Outputs a complete HTML document ready for WeChat
